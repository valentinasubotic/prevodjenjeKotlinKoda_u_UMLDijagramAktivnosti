fun proveri() {
    val broj = 10
    val rezultat = broj * faktor  // <- faktor 

    if (rezultat > prag) {        // <- prag
        println("Prešao prag!")
    }

    for (i in niz) {              // <- niz 
        println(i)
    }

    val zbir = a + b              // <-  a,b 
}
